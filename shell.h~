#ifndef SHELL_H
#define SHELL_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <string.h>
#include <stdarg.h>
#include <errno.h>

char **thetokenizer(char *buff, char delim);
char *xcmd(char **argv, char **env, char **sargs, int line, int *err);
void zchexe(char *cmd, char **sargs, char **env, int *err);
void thexitbuiltin(char *buff, char **argv, int line, int *err);
void the_envbltin(char *buff, char **env);
char *_cstrcpyfun(char *dest, char *src);
int _cstrcmpfun(char *s1, char *s2);
int _cstrncmpfun(char *s1, char *s2, int n);
char *_cstrcatfun(char *dest, char *src);
char *theprepbuff();
char *latepath(char **sargs, char **argv, int line);
void errorprint(char **argv, char **sargs, int line, int err);
void expfun(char *rel, char *dest);
char *trimerbuff(char *buff);
int _strlen(char *str);


/* THE USED PRINTF FUNCTIONS IN THIS CODE */

int _theprintf(int fd, const char *format, ...);
void _theputs(int fd, char *str);
int _aputchar(int fd, char c);
char *_theitoa(int value, char *buffer, int base);
int _theabs(int a);
char *_areverse(char *buffer, int i, int j);
void _cswap(char *x, char *y);
int print_aconve(int fd, const char *str, va_list arg);
int print_s(int fd, va_list arg);
int _atoii(char *s);

#endif
